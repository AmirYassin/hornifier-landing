// HORNIFIER landing page configuration.
// Edit these values once when the Lemon Squeezy product and trial builds are ready.
// All CTAs across the page read from this object — nothing else needs changing.
window.HORNIFIER_CONFIG = {
  // Lemon Squeezy checkout URL — leave empty ("") until the product is live.
  // When set, the Lemon Squeezy overlay (lemon.js) will open as an in-page modal.
  CHECKOUT_URL: "https://hornifier-audio.lemonsqueezy.com/checkout/buy/6a2a9c63-4556-4d92-9d40-e9113f07cf32",

  // Direct download URL for the trial build — leave empty ("") until ready.
  TRIAL_URL: "",

  // Displayed price (intro)
  PRICE: "$49",

  // Displayed price (full / before intro) — shown struck-through on pricing panel.
  PRICE_FULL: "$69"
};
